### 문제 1: 테이블 생성하기 (CREATE TABLE)
1. **`attendance` 테이블은 중복된 데이터가 쌓이는 구조이다. 중복된 데이터는 어떤 컬럼인가?**
   
   - crew_id, nickname
   
2. **`attendance` 테이블에서 중복을 제거하기 위해 `crew` 테이블을 만들려고 한다. 어떻게 구성해 볼 수 있을까?**

   - crew_id와 nickname을 컬럼으로 가지는 테이블을 만들고, attendance 테이블에서는 nickname 컬럼을 제거한다.
   - 그 후에 crew_id로 join을 하여 두 테이블을 연결하면 중복을 제거할 수 있다.
   
3. **`crew` 테이블에 들어가야 할 크루들의 정보는 어떻게 추출할까? (hint: DISTINCT)**

   ```MySQL
   SELECT DISTINCT crew_id, nickname FROM attendance;
   ```
   
4. **최종적으로 crew 테이블 생성:**

   ``` MYSQL
   CREATE TABLE crew (
      crew_id INT NOT NULL AUTO_INCREMENT,
      nickname VARCHAR(50) NOT NULL,
      PRIMARY KEY (crew_id)
   );
   ```

6. **attendance 테이블에서 크루 정보를 추출해서 crew 테이블에 삽입하기:**


   ```mysql
   INSERT INTO crew (nickname)
   SELECT DISTINCT nickname
   FROM attendance;
   ```

### 문제 2: 테이블 컬럼 삭제하기 (ALTER TABLE)
1. **crew 테이블을 만들고 중복을 제거했다. attendance에서 불필요해지는 컬럼은?**
   - nickname 컬럼
2. **컬럼을 삭제하려면 어떻게 해야 하는가?**
   ```mysql
   ALTER TABLE attendance
   DROP COLUMN nickname;
   ```

### 문제 3: 외래키 설정하기
```mysql
ALTER TABLE attendance
ADD CONSTRAINT fk_crew_id
FOREIGN KEY (crew_id) REFERENCES crew(crew_id)
ON DELETE RESTRICT
ON UPDATE CASCADE;
```

### 문제 4: 유니크 키 설정
```mysql
ALTER TABLE crew
ADD CONSTRAINT uk_nickname UNIQUE (nickname);
```

### 문제 5: 크루 닉네임 검색하기 (LIKE)
```mysql
SELECT * FROM crew WHERE nickname LIKE '디%';
```

### 문제 6: 출석 기록 확인하기 (SELECT + WHERE)
```mysql
SELECT *
FROM attendance a
JOIN crew c ON a.crew_id = c.crew_id
WHERE c.nickname = '어셔' AND a.attendance_date = '2025-03-06'
```

### 문제 7: 누락된 출석 기록 추가 (INSERT)
```mysql
INSERT INTO attendance (crew_id, attendance_date, entrance_time, exit_time)
VALUES (
    (SELECT crew_id FROM crew WHERE nickname = '어셔'),
    '2024-03-06', 
    '09:31:00', 
    '18:01:00'
);
```

### 문제 8: 잘못된 출석 기록 수정 (UPDATE)
```mysql
UPDATE attendance
SET start_time = '10:00'
WHERE crew_id = (SELECT crew_id FROM crew WHERE nickname = '주니')
   AND attendance_date = '2025-03-12';
```

### 문제 9: 허위 출석 기록 삭제 (DELETE)
```mysql
DELETE FROM attendance
WHERE crew_id = (SELECT crew_id FROM crew WHERE nickname = '아론')
   AND attendance_date = '2025-03-12';
```

### 문제 10: 출석 정보 조회하기 (JOIN)
```mysql
SELECT a.*, c.nickname
FROM attendance a
JOIN crew c ON a.crew_id = c.crew_id;
```

### 문제 11: nickname으로 쿼리 처리하기 (서브 쿼리)
```mysql
SELECT *
FROM attendance
WHERE crew_id = (SELECT crew_id FROM crew WHERE nickname = '입력값');
```

### 문제 12: 가장 늦게 하교한 크루 찾기
```mysql
SELECT c.nickname, a.end_time
FROM crew c
JOIN attendance a ON c.crew_id = a.crew_id
WHERE a.attendance_date = '2025-03-05'
ORDER BY a.end_time DESC
LIMIT 1;
```

### 문제 13: 크루별로 '기록된' 날짜 수 조회
```mysql
SELECT COUNT(attendace_date)
FROM attendance
GROUP BY crew_id;
```

### 문제 14: 크루별로 등교 기록이 있는(start_time IS NOT NULL) 날짜 수 조회
```mysql
SELECT crew_id, COUNT(attendance_date) AS '등교한 날짜 수'
FROM attendance
WHERE start_time IS NOT NULL
GROUP BY crew_id;
```

### 문제 15: 날짜별로 등교한 크루 수 조회
```mysql
SELECT attendance_date, COUNT(crew_id) AS '등교한 크루 수'
FROM attendance
GROUP BY attendace_date;
```

### 문제 16: 크루별 가장 빠른 등교 시각(MIN)과 가장 늦은 등교 시각(MAX)
```mysql
SELECT crew_id, MIN(start_time) AS '가장 빠른 등교 시각', MAX(end_time) AS '가장 늦은 등교 시각'
FROM attendance
GROUP BY crew_id;
```

## 생각해보기
### 기본키란 무엇이고 왜 필요한가?
기본키는 관계형 데이터베이스에서 테이블의 각 행을 고유하게 식별할 수 있는 하나 이상의 컬럼을 의미한다.
#### 기본키의 조건
1. Unique(유일성) : 테이블 안에 똑같은 기본키를 가진 행은 존재할 수 없다.
2. Not Null : 기본키인 컬럼은 절대 NULL일 수 없다.
#### 기본키가 필요한 이유(식별자가 없을 경우 생기는 문제)
1. **중복 데이터 발생**

   닉네임이 '에버'인 크루가 두 명 있다고 가정할 때, 기본키가 없다면 DB 입장에서는 이 두 크루를 구분할 방법이 없다.
   
2. **정확한 수정 & 삭제 불가**

   '에버'의 출석 기록을 수정하거나 삭제하고 싶을 때, 원하는 '에버'를 특정할 수 없다.
   결국 '에버'라는 닉네임을 가진 모든 크루의 데이터가 변경될 가능성이 있다.
   
3. **테이블 합성 불가**

   기본키가 없으면 테이블 간 합성을 할 수 없고, 데이터는 각각 파편화되어 고립된다.

### MySQL에서 사용되는 `AUTO_INCREMENT`는 왜 필요할까?
1. **번거로움**
   
   새로운 데이터를 저장할 때마다 마지막 번호가 몇 번이었는지 기억하고 1을 더해서 넣는 작업은 매우 비효율적이다.
   또한 마지막 번호를 찾기 위해 매번 테이블을 스캔해야 한다면 데이터가 쌓일수록 성능이 저하된다.
2. **중복 발생**
   
   여러 사람이 동시에 데이터를 저장할 때 직접 번호를 입력해야 한다면, 중복된 번호로 데이터가 저장될 수 있다.

기본키는 절대 중복이면 안 되고 NULL이어서도 안 된다. AUTO_INCREMENT는 이 두 가지 조건을 완벽하게 만족하는 값을 자동으로 생성해주기 때문에 필요하다.

### 학생이 등교는 했지만 하교 버튼을 누르지 않았을 때, end_time에 NULL이 저장된다. NULL 값을 처리할 때 주의할 점은?
1. **SQL 연산에서 주의할 점**
   
   `=`연산자 사용 금지 : `WHERE end_time = NULL'이라고 쓰면 아무런 결과도 나오지 않는다. NULL은 비교 대상이 아니기 때문이다.
   반드시 `IS NULL` 또는 `IS NOT NULL`을 사용해야 한다.

3. **산술 및 집계 함수에서의 주의할 점**
   
   - 산술 연산 : `NULL`에 어떤 숫자를 더하거나 빼도 결과는 무조건 NULL이다.(100 + NULL = NULL)
   - 집계 함수 : `AVG()`, `SUM()`, `COUNT(column)` 등은 `NULL`값을 무시하고 계산한다.
   위와 같은 특징은 통계적 오류를 불러올 수 있으므로 `IFNULL()`이나 `COALESCE()`로 기본값을 채워주는 처리가 필요할 때가 있다.

5. **프론트엔드 및 실무적 처리에서 주의할 점**
   
   데이터를 사용자에게 보여줄 때 `NULL`을 그대로 노출하면 사용자 경험이 떨어진다.
   - 대체 문구 노출 : 프론트엔드에서는 `null`이나 `undefined`를 체크하여 대체 문구로 치환해야 한다.
   - 데이터 타입 에러 방지 : API 응답으로 `end_time : null`이 들어올 때, 클라이언트 코드에서 해당 값에 함수를 호출(`end_time.split(':')`하면 런타임 에러가 발생한다. 반드시 Optional Chaining이나 조건문을 통한 방어 코드가 필요하다.
  
### crew와 attendance 테이블의 관계를 ER 다이어그램으로 시각화해보자. 이 관계를 일상 생활의 예시로 비유한다면 어떤 것이 있을까?
crew와 attendance는 일대다(1:N) 관계이다. 한 명의 크루는 여러 출석 기록을 가지고 있을 수 있다.
**일상 속의 일대다 관계**
1. 출판사 - 도서
2. 인스타그램 사용자 - 게시물
3. 카페 고객 - 쿠폰 스탬프

### 출석 시스템에서 동시에 100명이 등교 버튼을 누른다면 어떤 일이 일어날까? 이 문제를 2026 공통강의 - DB에서 배운 트랜잭션과 ACID 속성으로 설명해보자.
1. **INSERT와 UPDATE가 일으키는 동시성 문제**
   - INSERT 시 : 중복 데이터와 순서 보장 문제
     - 유니크 충돌 : 만약 '에버'라는 닉네임에 `UNIQUE` 제약 조건이 걸려 있는데, 찰나의 순간에 두 번의 `INSERT` 요청이 들어가면 시스템이 판단 오류를 일으킬 수 있다.
     - 팬텀 리드(Phantom Read) : 한 트랜잭션이 출석 인원을 세고 있는데, 그 사이 다른 트랜잭션이 새로운 데이터를 `INSERT`해버리면 결과값이 순식간에 바뀌어 데이터 불일치가 발생한다.
    
   - UPDATE 시 : 갱신 손실(Lost Update) 문제
     - 가장 위험한 문제이다. 100명이 동시에 '오늘 총 출석 인원' 컬럼을 +1씩 업데이트한다고 가정해보자.
     - 상황 : A와 B가 동시에 현재 인원 '50명'을 읽었다.
     - 문제 : A가 +1을 해서 '51'을 저장하고, 0.001초 뒤에 B도 자기가 읽었던 '50'에 +1을 해서 '51'을 덮어쓴다.
     - 결과 : 실제로는 2명이 늘었으니 '52'가 되어야 하지만, 한 명의 기록이 씹히는 **갱신 손실**이 발생한다.
    
2. **원자성이 필요한 이유**
   - 부분 성공 방지 : 출석 버튼을 누르면 `attendance` 테이블에 행이 추가되고, `crew` 테이블의 '최근 출석일' 정보가 갱신되어야 한다고 해보자.
   - 사고 예방 : 만약 행은 생겼는데 시스템 오류로 '최근 출석일' 갱신에 실패한다면? 데이터가 짝이 맞지 않는 유령 데이터가 된다. 원자성이 보장되면 두 작업 중 하나라도 실패 시 전체를 취소하여 데이터의 결점이 생기는 것을 막아준다.
  
3. **격리성이 필요한 이유**
   - 중간 과정 보호 : 100명이 동시에 몰릴 때, 각 트랜잭션이 서로의 수정 중인 데이터를 들여다보지 못하게 차단한다.
   - 데이터 정합성 유지 : '갱신 손실'이나 '팬텀 리드'를 방지하기 위해, 특정 트랜잭션이 데이터를 건드리는 동안 다른 트랜잭션은 잠시 기다리게 하거나, 데이터의 특정 시점 스냅샷만 보게 함으로써 100명의 데이터가 차례대로 정확하게 반영되도록 보장한다.

### 출석 데이터가 파일(CSV)이 아닌 데이터베이스에 저장되는 이유는 무엇일까? 파일 시스템으로 출석을 관리했다면 어떤 문제가 생길까?
1. **동시 접근의 한계**
   - 파일 시스템 : 파일은 보통 한 번에 한 명만 수정할 수 있도록 락(Lock)이 걸린다. 누군가 파일을 쓰고 있으면 다른 사람은 기다려야 하거나, 운이 나쁘면 파일 자체가 깨져버린다. 100명이 줄 서서 한 명씩 파일이 저장되길 기다리느라 10시 5분이 넘어버릴지도 모른다.
   - 데이터베이스 : 격리성과 잠금 메커니즘을 통해 100명의 요청을 아주 짭은 찰나에 안전하게 순차적으로 처리한다.
  
2. **데이터 중복과 일관성 문제**
   - 파일 시스템 : csv 파일에 매번 `에버, 2026-03-06, 09:31...` 식으로 저장한다면, 에버의 닉네임이 바뀌었을 때 파일 내 수백 개의 행을 일일이 찾아 고쳐야 한다. 하나라도 놓치면 누구는 '에버' 누구는 '에에버어'로 남는 데이터 불일치가 발생한다.
   - 데이터베이스 : `crew` 테이블과 `attendance` 테이블을 분리(정규화)하고 ID로 연결한다. 닉네임이 바뀌면 `crew` 테이블의 단 한 줄만 수정하면 끝난다.
  
3. **무결성 제약 조건의 부재**
   - 파일 시스템 : csv 파일은 텍스트일 뿐이다. 누군가 실수로 하교 시간에 숫자가 아닌 문자를 적어도 파일은 거부하지 않는다.
   - 데이터베이스 : 컬럼의 데이터 타입과 제약 조건이 해당 문제를 예방한다. 잘못된 형식의 데이터가 들어오려고 하면 거부한다.
  
4. **보안과 권한 관리**
   - 파일 시스템 : csv 파일에 접근 권한이 있는 사람이라면 누구나 모든 데이터를 복사하거나 삭제할 수 있다. 특정 컬럼만 가리고 보여주는 세밀한 관리가 매우 어렵다.
   - 데이터베이스 : 사용자별로 SELECT, INSERT, UPDATE 권한을 세밀하게 부여할 수 있다. 코치는 모든 데이터를 보고, 크루는 자기 기록만 보게 만드는 권한 설정이 매우 간단하다.
  
5. **검색 속도의 차이**
   - 파일 시스템 : 1년 치 출석 데이터가 100만 건쯤 쌓였을 때, '에버의 3월 6일 기록'을 찾으려면 파일의 첫 줄부터 끝까지 다 읽어야 한다.(O(n))
   - 데이터베이스 : 인덱스를 사용하면 수백만 건 중에서도 원하는 데이터를 눈 깜짝할 새(O(logn))에 찾아낸다.
  
### 출석 데이터를 관계형 DB가 아닌 NoSQL(예: MongoDB)로 저장한다면 테이블 구조가 어떻게 달라질까? 어떤 장단점이 있을까?
#### 테이블 구조의 변화
1. **임베딩 방식 (Embedding)**
   - 크루 문서 안에 출석 기록 리스트를 아예 통째로 집어넣는 방식이다.
     ```json
     {
        "_id": "crew_01",
        "nickname": "에버",
        "attendance_records": [
          { "date": "2026-03-04", "entrance": "09:30", "exit": "18:01" },
          { "date": "2026-03-06", "entrance": "09:31", "exit": null }
        ]
      }
     ```
2. **참조 방식**
   - RDBMS와 비슷하게 컬렉션을 나누고 ID만 저장한다. 하지만 DB 차원의 JOIN 기능이 약하거나 없다.
     ```json
     // attendance collection
      {
        "crew_id": "crew_01",
        "date": "2026-03-04",
        "entrance": "09:30"
      }
     ```

#### NoSQL로 관리할 때의 장단점
1. **유연성**
   - 장점 : 닉네임 외에 SNS 주소, MBTI 등 새로운 필드를 추가할 때 테이블 구조 변경(Alter) 없이 즉시 저장 가능하다.
   - 단점 : 데이터 형식이 제각각일 수 있어, 애플리케이션 레벨에서 데이터를 검증하는 로직이 복잡해진다.
2. **성능**
   - 장점 : 임베딩 방식 사용 시, 크루 정보와 출석 기록을 한 번의 쿼리로 가져올 수 있어 읽기 속도가 매우 빠르다.
   - 단점 : 데이터 중복이 발생하기 쉽고, 수정 시 모든 중복 데이터를 일일이 찾아 고쳐야 하는 번거로움이 있다.
3. **확장성**
   - 장점 : 서버를 여러 대로 나누는 수평 확장(Sharding)이 RDBMS보다 훨씬 유리하다.
   - 단점 : 트랜잭션(ACID) 처리가 RDBMS만큼 강력하지 않아, 결제나 출석 정합성이 중요한 시스템에선 위험할 수 있다.
  
#### 구조가 명확한 데이터 vs 자유로운 데이터
- 이 선택의 핵심은 **데이터가 얼마나 자주 변하고, 얼마나 엄격해야 하는가**에 있다.
  - **출석 데이터(구조 명확)** : 날짜, 입실, 퇴실 등 형식이 고정되어 있고, 1분 1초의 정합성이 중요하다. 이런 경우 RDBMS가 훨씬 안전하고 관리가 편하다. 외래키 제약 조건을 통해 "없는 크루의 출석 기록"이 생기는 것을 원천 봉쇄할 수 있기 때문이다.
  - **크루 프로필(자유로운 구조)** : 어떤 크루는 기술 블로그 주소를 적고 싶어 하고, 어떤 크루는 자기소개 한 줄만 적고 싶어 한다. 나중에 "좋아하는 음식" 필드가 추가될 수도 있다. 이렇게 요구사항이 계속 변하고 필드가 들쭉날쭉한 데이터는 NoSQL이 압도적으로 유리하다.

 ### 왜 crew 테이블에서 nickname을 기본키로 하지 않은 걸까? attendance 테이블에 attendance_id가 존재하는 이유는 무엇일까?
 1. **왜 `nickname`을 기본키(PK)로 쓰지 않을까?**
    - 닉네임은 실생활에서 의미를 갖는 자연키이다. 하지만 자연키는 기본키의 가장 중요한 덕목인 **불변성**에서 탈락할 확률이 높다.
    - **변경의 위험성** : "우테코에서는 닉네임 중복이 금지된다"는 규칙은 현재의 운영 방침일 뿐이다. 만약 크루가 닉네임을 바꾸고 싶어 한다면? PK인 닉네임을 바꾸는 순간, 이 닉네임을 참조하던 수많은 출석 기록(attendance 테이블의 FK)들도 줄줄이 비상이 걸린다.
    - **비즈니스 규칙의 가변성** : 나중에 시스템이 확장되어 "졸업생과 재학생은 닉네임이 겹쳐도 된다"는 식으로 규칙이 바뀐다면, 닉네임은 더 이상 PK의 자격을 유지할 수 없게 된다.
    - **저장 공간과 성능** : 문자열(String)은 숫자(Integer)보다 용량을 더 많이 차지한다. 또한, 인덱스를 타고 데이터를 찾거나 테이블을 조인(Join)할 때 숫자를 비교하는 것이 문자열을 비교하는 것보다 훨씬 빠르다.
   
2. **`attendance_id`가 존재하는 이유는 무엇일까?**
   - 출석 기록 테이블에서 특정 행 하나를 완벽하고 유일하게 집어내기 위해서이다.
   - **복합키의 불편함** : `attendance` 테이블에서 `crew_id`와 `attendance_date`를 묶어서 기본키로 쓸 수도 있다(복합키). 하지만 이렇게 하면 나중에 이 출석 기록을 다른 테이블에서 참조할 때마다 두 개의 컬럼을 모두 넘겨줘야 하므로 쿼리가 복잡해진다.
   - **데이터의 유일성 보장** : 만약 어떤 오류로 인해 같은 날 한 크루의 출석 기록이 두 번 들어갔다고 치자. `attendance_id`가 없다면 어떤 게 잘못된 데이터인지 구분하기 매우 어렵다. 숫자로 된 고유 ID가 있으면 "105번 행이 잘못됐으니 지워줘"라고 명확히 지시할 수 있다.
   - **관리의 일관성** : 모든 테이블에 일관되게 `id` 형태의 대리키를 두면, 시스템 전체의 구조가 단순해지고 유지보수가 쉬워진다.
  
### 데이터베이스 제약 조건 중 RESTRICT, CASCADE는 무엇인가?
1. **RESTRICT(엄격한 룰)**
  - 동작: 부모 레코드를 삭제하거나 수정하려 할 때, 이를 참조하는 자식 레코드가 하나라도 존재하면 해당 명령을 거부한다.
  - 특징: 데이터의 안전성을 최우선으로 한다. 실수로 부모 데이터를 지워서 자식 데이터가 '고아'가 되는 상황을 원천 봉쇄한다.
  - 비유: 학급(부모)에 학생(자식)이 단 한 명이라도 남아있다면, 담임 선생님은 퇴근할 수 없는 상황과 비슷하다.
2. **CASCADE(도미노 효과)**
  - 동작: 부모 레코드가 삭제되면 이를 참조하는 자식 레코드도 자동으로 함께 삭제된다. 수정(Update) 시에도 부모의 PK 값이 바뀌면 자식의 FK 값도 자동으로 따라 바뀐다.
  - 특징: 관리가 매우 편리하고 데이터 정리가 깔끔하다. 하지만 실수로 부모를 지웠을 때 연쇄적으로 엄청난 양의 데이터가 날아갈 수 있어 신중해야 한다.
  - 비유: 인스타그램 계정(부모)을 삭제하면 내가 올린 모든 게시물(자식)이 한꺼번에 사라지는 것과 같다.

### 다음 두 쿼리는 동일한 결과를 반환하지만 성능에 차이가 있다. 어떤 차이가 있으며, 어떤 상황에서 각각 유리할까?
```mysql
-- 쿼리 1: 서브쿼리 사용
SELECT * FROM attendance WHERE crew_id IN (SELECT crew_id FROM crew WHERE nickname LIKE '네%');

-- 쿼리 2: JOIN 사용
SELECT a.* FROM attendance a JOIN crew c ON a.crew_id = c.crew_id WHERE c.nickname LIKE '네%';
```
1. **쿼리 1: 서브쿼리 (`IN`) 방식**
   - 이 방식은 **필터링**에 특화되어 있다. crew 테이블에서 '네'로 시작하는 크루 ID들을 먼저 뽑아낸 뒤, 그 리스트에 포함된 출석 기록만 가져온다.
   - **작동 방식**: 서브쿼리의 결과 집합을 먼저 만든 후, 메인 쿼리의 데이터를 하나씩 대조합니다. (최신 DB에서는 이를 Semi-join으로 최적화하여 처리하기도 합니다.)
   - **장점**:
     - **가독성**: "특정 그룹에 속한 데이터를 뽑는다"는 의도가 명확히 드러납니다.
     - **중복 방지**: attendance 테이블의 데이터만 가져오므로, 조인 시 발생할 수 있는 데이터 뻥튀기(중복) 걱정이 없습니다.
     - **단점**: * 서브쿼리의 결과가 너무 크면(예: 수만 건의 ID), IN 절에 담긴 리스트를 체크하는 비용이 커져 성능이 급격히 떨어질 수 있습니다.
    
2. **쿼리 2: `JOIN` 방식**
   - 이 방식은 두 테이블을 **결합**한 뒤 조건에 맞는 행을 골라낸다.
   - **작동 방식**: 두 테이블을 인덱스(Index)를 기반으로 하나로 합친다. DB 엔진은 보통 데이터가 적은 테이블을 먼저 읽고(Driving Table), 인덱스를 타고 상대 테이블(Driven Table)을 찾는 방식을 사용한다.
   - **장점**:
     - **성능**: 대용량 데이터 처리 시 일반적으로 서브쿼리보다 빠르다. 특히 `crew_id`에 인덱스가 잘 걸려 있다면 DB 엔진이 가장 효율적인 경로를 찾아내기 쉽다.
     - **확장성**: `attendance` 컬럼뿐만 아니라 `crew` 테이블의 컬럼(예: 닉네임)도 한꺼번에 조회해야 할 때 유리하다.
   - **단점**:
     - 조인 조건이 잘못되면 예상보다 훨씬 많은 양의 데이터가 임시로 생성되어 메모리를 점유할 수 있다.
    
### attendance 테이블을 완전히 정규화하면 어떤 장점이 있을까? 반대로 일부 비정규화를 적용한다면 어떤 쿼리 성능 이점을 얻을 수 있을까?
1. **완전 정규화의 장점: "데이터의 무결성과 효율적 관리"**
   - `attendance` 테이블에서 크루의 이름이나 이메일 같은 정보를 떼어내고 오직 `crew_id`만 남기는 방식이다.
   - **데이터 중복 제거**: 동일한 크루 정보가 수천 번 중복 저장되는 것을 막아 저장 공간을 절약한다.
   - **이상 현상(Anomalies) 방지**:
     - **수정 이상**: 크루가 닉네임을 바꿨을 때, `crew` 테이블 한 곳만 수정하면 모든 출석 기록에 자동으로 반영된다.
     - **삭제 이상**: 출석 기록을 지운다고 해서 크루의 신상 정보까지 같이 사라지는 비극을 막는다.
   - **유연한 구조**: 새로운 데이터(예: 크루의 생일)가 추가되어도 `attendance` 테이블의 구조를 건드릴 필요가 없다.
  
2. **비정규화의 장점: "읽기 성능의 극대화"**
   - 성능을 위해 일부러 규칙을 어기고 `attendance` 테이블에 크루의 `nickname`을 중복해서 저장하는 방식이다.
   - **JOIN 비용 감소**: 가장 큰 이점이다. "오늘 출석한 크루들의 닉네임"을 조회할 때, crew 테이블을 찾아갈 필요 없이 attendance 테이블 하나만 읽으면 끝난다.
     - 데이터가 수억 건일 때, JOIN 연산은 CPU와 메모리에 상당한 부담을 주는데 이를 원천 차단한다.
   - **쿼리 단순화**: 복잡한 서브쿼리나 JOIN 문을 작성할 필요가 없어 코드가 간결해지고, 인덱스 설계가 단순해진다.
   - **조회 성능 향상**: 특정 시점의 데이터를 보존해야 할 때 유리하다. 예를 들어, 크루가 나중에 닉네임을 바꾸더라도 "당시 출석부에는 이 닉네임으로 찍혀 있었다"는 기록을 남겨야 한다면 비정규화가 정답이 될 수 있다.

### 출석 시스템이 수백 명의 사용자에 의해 동시에 접근된다면, 연결 풀링(connection pooling)은 무엇이고 왜 필요한가?
1. **연결 풀링(Connection Pooling)이란?**
   - 쉽게 비유하자면, 카페에서 손님이 올 때마다 매번 새로운 직원을 채용(연결 생성)하는 게 아니라, **미리 일정 수의 직원(연결)을 고용해서 대기시켜 두는 직원 대기실**과 같다.
   - 애플리케이션이 실행될 때 데이터베이스와의 연결을 미리 여러 개 만들어 '풀(Pool)'이라는 공간에 담아두고, 요청이 올 때마다 이 연결을 빌려줬다가 작업이 끝나면 다시 반납받는 방식이다.
  
2. **왜 필요한가? (생성과 삭제의 고통)**
   - 데이터베이스와 연결을 맺는 과정은 생각보다 훨씬 무겁고 복잡한 작업이다.
     1. **네트워크 연결**: TCP/IP 연결을 맺어야 한다.
     2. **인증**: 아이디, 비밀번호를 확인하고 권한을 체크한다.
     3. **자원 할당**: DB 엔진 내부에서 이 연결을 위한 메모리와 세션을 할당한다.
   - 만약 수백 명의 크루가 동시에 버튼을 누를 때마다 이 과정을 반복한다면?
     - **응답 속도 저하**: 연결을 맺는 데 걸리는 시간 때문에 정작 쿼리 실행은 시작도 못 하고 대기하게 된다.
     - **서버 과부하**: 연결 생성/삭제 자체가 CPU와 메모리를 많이 잡아먹어 서버 전체가 느려진다.
     - **연결 고갈**: 데이터베이스가 수용할 수 있는 최대 연결 수(Max Connections)를 순식간에 초과하여, 이후 요청은 모두 에러가 발생한다.
    
3. **연결 풀링의 작동 원리**
   1. **미리 생성 (Setup)**: 애플리케이션 시작 시 설정된 개수(예: 10개)만큼 DB 연결을 생성해 풀에 넣어둔다.
   2. **대여 (Borrow)**: 사용자가 버튼을 누르면 풀에서 유휴(Idle) 상태의 연결을 하나 꺼내준다.
   3. **사용 (Execute)**: 빌려온 연결로 SQL(INSERT, UPDATE 등)을 실행한다.
   4. **반납 (Return)**: 작업이 끝나면 연결을 끊는 게 아니라, 다시 풀에 넣어서 다음 사람이 쓸 수 있게 한다.
  
4. **실무적인 이점**
   - **성능 향상**: 이미 맺어져 있는 연결을 즉시 사용하므로 응답 속도가 비약적으로 빨라진다.
   - **자원 관리**: 최대 연결 수를 제한할 수 있어, 갑작스러운 트래픽 폭주에도 DB 서버가 뻗지 않도록 방어막 역할을 한다.
   - **안정성**: 연결 상태를 주기적으로 체크하여 끊긴 연결은 자동으로 복구해 주는 기능도 포함되어 있다. (예: HikariCP 등)
